/// User input is assign to this forms has collacted theme
class RegistrationCredentials {
  RegistrationCredentials(
      {this.userEmail, this.userPassword, this.confirmPaasword, this.userName});
  String userEmail;
  String userPassword;
  String confirmPaasword;
  String userName;
}
